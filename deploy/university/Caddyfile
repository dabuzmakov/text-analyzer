{
    email {$ACME_EMAIL}
}

{$DOMAIN_NAME} {
    encode zstd gzip

    @api path /api/*
    handle_path /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }

    log {
        output stdout
        format console
    }
}
