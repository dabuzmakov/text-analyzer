name: CI

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  database:
    name: Database Schema Check
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: text_analyzer
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d text_analyzer"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Apply DB migration
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/text_analyzer
        run: bash scripts/db/apply-migrations.sh

      - name: Validate required DB objects
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d text_analyzer -v ON_ERROR_STOP=1 -c "
          DO \$\$
          DECLARE
            required_count INTEGER;
            trigger_count INTEGER;
            function_count INTEGER;
          BEGIN
            SELECT COUNT(*) INTO required_count
            FROM information_schema.tables
            WHERE table_schema = 'public'
              AND table_name IN (
                'app_clients',
                'documents'
              );

            IF required_count <> 2 THEN
              RAISE EXCEPTION 'Schema validation failed: expected 2 required tables, got %', required_count;
            END IF;

            SELECT COUNT(*) INTO trigger_count
            FROM pg_trigger
            WHERE NOT tgisinternal
              AND tgname = 'trg_documents_limit_per_client_before_insert';

            IF trigger_count <> 1 THEN
              RAISE EXCEPTION 'Schema validation failed: expected trigger trg_documents_limit_per_client_before_insert';
            END IF;

            SELECT COUNT(*) INTO function_count
            FROM pg_proc
            WHERE proname IN (
              'get_client_id_by_browser_id',
              'get_client_documents',
              'get_client_documents_by_browser_id'
            );

            IF function_count <> 3 THEN
              RAISE EXCEPTION 'Schema validation failed: expected 3 helper SQL functions, got %', function_count;
            END IF;
          END
          \$\$;
          "

  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

  backend:
    name: Backend Smoke Check
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: text_analyzer
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d text_analyzer"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/text_analyzer
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile backend entrypoint
        run: python -m compileall main.py services

      - name: Import ASGI app
        run: python -c "from main import app; assert app.title == 'Лексема API'"
